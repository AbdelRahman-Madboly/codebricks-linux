# Notes — CH2: Filesystems
> Fill in the answers yourself, in your own words.
> Rule: if you copy-paste an answer, it doesn't count.

## Questions

1. What is the difference between an absolute path and a relative path — and when would using the wrong one break a script?
   > YOUR ANSWER:

2. `cat`, `head`, `less` all let you view file contents. What situation calls for each one specifically?
   > YOUR ANSWER:

3. `mv` can both rename and move a file. What is actually happening under the hood in both cases — is it the same operation?
   > YOUR ANSWER:

4. What is the difference between `rm -r` and `cp -R`? Why does one use lowercase `-r` and the other uppercase `-R`?
   > YOUR ANSWER:

5. `grep` and `find` both "search" — but for completely different things. Explain the difference and give a real example where you would reach for each one.
   > YOUR ANSWER:

6. `touch` is described as updating a file's timestamp, but it is mostly used to create empty files. Why would you ever need to update a timestamp without changing file content?
   > YOUR ANSWER:

7. Why is your home directory (`~`) the recommended place to do all your development work, rather than somewhere like `/usr` or `/etc`?
   > YOUR ANSWER:

## Commands I Keep Forgetting
> Fill this in as you practice — leave blank now.

## My Examples
> Write your own real use cases here — leave blank now.

## Connections to My Work
> How does this connect to your AI engineering work? — leave blank now.
